export default function inadimplencia({ store, next }) {
  return new Promise((resolve, reject) => {
    // if(store.getters.getEmpresa.fatura.length) {
    //   reject(next({ name: 'PaymentBilling' }))
    // }
    resolve()
  })
}
